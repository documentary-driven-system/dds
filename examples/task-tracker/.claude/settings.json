{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit*)",
            "timeout": 120,
            "command": "python \"${CLAUDE_PROJECT_DIR}/.dds/meta/scripts/dds.py\" --root \"${CLAUDE_PROJECT_DIR}\" check --gate || { echo \"DDS gate: clear the ERROR lines above, then commit again (.dds/meta/manifesto.dds.md [1].3).\" >&2; exit 2; }"
          },
          {
            "type": "command",
            "if": "Bash(git -C * commit*)",
            "timeout": 120,
            "command": "python \"${CLAUDE_PROJECT_DIR}/.dds/meta/scripts/dds.py\" --root \"${CLAUDE_PROJECT_DIR}\" check --gate || { echo \"DDS gate: clear the ERROR lines above, then commit again (.dds/meta/manifesto.dds.md [1].3).\" >&2; exit 2; }"
          }
        ]
      }
    ]
  }
}
