# Archive

Retired documents live here, in the same folder layout as their original tier (`product/`, `architecture/`, `modules/`).
Nothing here is indexed by a tree; Tombstone lines in the trees are the entry points. Files keep their original `id` forever.
